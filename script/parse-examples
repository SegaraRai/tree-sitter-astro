#!/usr/bin/env sh

set -eu

cd "$(dirname "$0")/.."

if command -v pwsh >/dev/null 2>&1; then
	exec pwsh -NoProfile -File script/parse-examples.ps1
fi

echo "pwsh not found. Install PowerShell (pwsh) or run script/parse-examples.ps1 directly." >&2
exit 1
